# Copyright (C) 2007 Robotics at Maryland
# Copyright (C) 2007 Joseph Lisee <jlisee@umd.edu>
# All rights reserved.
#
# Author: Joseph Lisee <jlisee@umd.edu>
# File:  packages/carnetix/SConscript

# Build system imports
import libs
import helpers

# Copy Our Build Environment
Import('env')
envl = env.Clone()

# Build Library (will be called 'libram_carnetix.so')
envl.Append(CPPPATH = ['include'])
helpers.SharedLibrary(envl, 'carnetix',  source = ['src/ctxapi.c'])

# Testing and Demo Programs
envt = env.Clone()

# Freaking build system is broken, shouldn't need this lib hack
envt.Append(CPPPATH = ['include'])

# Build the ctx and consolemon
helpers.Program(envt, target = 'ctx', source = 'test/src/ctx.c',
                int_deps = 'carnetix')

helpers.Program(envt, target = 'consolemon', source = 'test/src/consolemon.c',
                int_deps = 'carnetix')
