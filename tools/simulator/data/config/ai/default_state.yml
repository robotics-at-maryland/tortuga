ram.ai.new.mainState.Body:
    start:
        type: Start
        transitions:
            next: test
    test:
        type: TesterState
        transitions:
            next: forward  

    forward:
        type: Forward
        init:
            distance: 2
        transitions:
            next: strafe

    strafe:
        type: Strafe
        init:
            distance: 2
        transitions:
            next: end

    end:
        type: End

